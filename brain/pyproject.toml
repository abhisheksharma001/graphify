[project]
name = "graphify-brain"
version = "0.1.0"
description = "Open-source Vapi call analytics with plain-English patterns"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "typer>=0.12",
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "baml-py>=0.75",
]

[project.scripts]
graphify-brain = "graphify_brain.cli:app"

[dependency-groups]
dev = [
    "pytest>=8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/graphify_brain"]

[tool.pytest.ini_options]
testpaths = ["tests"]
